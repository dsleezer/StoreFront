@model StoreFront.DATA.EF.Models.Genre

@{
    ViewData["Title"] = Model.Genre1 + " Delete";
}

<h2>@ViewData["Title"]</h2>

<h3>Are you sure you want to delete @Model.Genre1? This cannot be undone.</h3>
<div>
    <h4>Genre</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Genre1)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Genre1)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="GenreId" />
        <input type="submit" value="Delete" class="btn btn-default" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
